# Charon Database

Manifest database for Charon.

- `database-1`: primary loose Lua, loose manifest, and ZIP source.
- `database-2`: secondary loose Lua, loose manifest, and ZIP source.

Each folder has an `index.json` generated from loose Lua files and ZIP contents.
